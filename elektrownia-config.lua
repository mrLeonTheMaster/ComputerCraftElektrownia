-- ID's of redrouter blocks, if the name of the redrouter block is 'redrouter_5', the id will be 5
engineControllerId = 0 -- The block controlling the steam engine
energyGeneratorControllerId = 0 -- The block controlling the power generators
fuelFactoryControllerId = 0 -- The block controlling the fuel factory

-- End of config, don't edit beyond this point.

return { engineControllerId = engineControllerId, energyGeneratorControllerId = energyGeneratorControllerId, fuelFactoryControllerId = fuelFactoryControllerId }